// Package store will hold the broker's persistence boundary (SQLite
// event-log store, schema and migrations) once Phase 2 wires it (see
// structure/folder-structure). Declared now, empty, to establish the
// module's foundational package layout.
package store
