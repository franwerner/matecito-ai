// Package ingest will hold the broker's hook ingestion and transcript-tail
// logic once Phase 7 wires it (see structure/folder-structure). Declared
// now, empty, to establish the module's foundational package layout.
package ingest
