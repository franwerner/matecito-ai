<!-- Canonical template: índice de un TIPO (`.matecito-ai/development-specs/<type>/INDEX.md`). Lista los capability-specs de ese tipo y cuándo consultarlos. Consumido por development-spec-bootstrap (materialización) y actualizado por sdd-archive. -->

# Capability specs — `<type>`

<1 línea: qué clase de comportamiento agrupa este tipo. Ej para `flow`: "Operaciones de cara a un actor, con pasos, ramas y casos borde.">

**Cuándo consultar este tipo:** <cuándo un dev debería abrir un spec de acá antes de trabajar. Ej: "antes de tocar cualquier endpoint o pantalla que dispare una operación de usuario".>

## Capacidades

| Capacidad | Qué hace | Status | Spec |
|---|---|---|---|
| `<capability>` | <una línea del comportamiento> | <Inferred \| Draft \| Accepted \| Deprecated> | [`<capability>.md`](<capability>.md) |

## Retiradas / reemplazadas

(Capabilities `Deprecated` de este tipo. Omitir la sección si no hay ninguna.)

| Capacidad | Reemplazada por |
|---|---|
| `<capability>` | [`<capability>.md`](<capability>.md) o "—" si se retiró sin reemplazo |
